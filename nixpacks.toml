[phases.setup]
nixPkgs = ["nodejs-18_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "./node_modules/.bin/tsc -p tsconfig.build.json 2>&1 || true"
]

[start]
cmd = "node dist/main"
