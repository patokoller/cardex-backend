[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npx prisma generate && npm run build:nest"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
