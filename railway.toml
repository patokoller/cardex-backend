[build]
builder = "NIXPACKS"
buildCommand = "echo 'Cache bust v4' && npx prisma generate && npm run build:nest"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
